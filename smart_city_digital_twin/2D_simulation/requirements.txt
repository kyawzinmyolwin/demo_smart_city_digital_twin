# traffic_counts_parser.py — Excel backends
openpyxl>=3.0.0
xlrd>=2.0.0

# match_intersection_opendata.py — NZTM (EPSG:2193) → WGS84
pyproj>=3.6.0

# run_traci.py --emit — live vehicle-state WebSocket emitter (Phase 1)
websockets>=13.0

# metrics_writer.py — writes per-tick metrics to InfluxDB (Phase 2, local dev)
influxdb-client>=1.40.0
